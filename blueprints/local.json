{
	"$schema": "https://playground.wordpress.net/blueprint-schema.json",
	"landingPage": "/wp-admin/post-new.php",
	"login": true,
	"steps": [
		{
			"step": "defineWpConfigConsts",
			"consts": {
				"WP_DEBUG": true,
				"WP_DEBUG_DISPLAY": false,
				"SCRIPT_DEBUG": true
			}
		},
		{
			"step": "activatePlugin",
			"pluginName": "Gutenberg Sync Engines",
			"pluginPath": "gutenberg-sync-engines/gutenberg-sync-engines.php"
		},
		{
			"step": "runPHP",
			"code": "<?php require_once '/wordpress/wp-load.php'; $experiments = (array) get_option( 'gutenberg-experiments', array() ); $experiments['gutenberg-real-time-collaboration'] = true; update_option( 'gutenberg-experiments', $experiments ); if ( ! username_exists( 'editor' ) ) { wp_insert_user( array( 'user_login' => 'editor', 'user_pass' => 'password', 'user_email' => 'editor@example.com', 'display_name' => 'Editor', 'role' => 'editor' ) ); } global $wpdb; foreach ( array( 'admin', 'editor' ) as $login ) { $user = get_user_by( 'login', $login ); if ( $user ) { update_user_meta( $user->ID, $wpdb->get_blog_prefix() . 'persisted_preferences', array( 'core/edit-post' => array( 'welcomeGuide' => false ), '_modified' => gmdate( 'c' ) ) ); } }"
		}
	]
}
