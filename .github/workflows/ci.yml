name: CI

on:
  push:
    branches: [ main ]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  js:
    name: Lint, typecheck, Jest
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            gutenberg/package-lock.json

      - name: Install plugin dependencies
        run: npm ci

      # Jest and typecheck resolve @wordpress/sync + yjs from the vendored
      # subtree, which is committed source-only. --ignore-scripts skips the
      # subtree's husky prepare hook (it errors outside a git root); the
      # lifecycle outputs it also skips are regenerated by `npm run build`.
      - name: Install and build the vendored Gutenberg subtree
        run: |
          cd gutenberg
          npm ci --ignore-scripts
          npm run build

      # PHP lint (composer lint) is deliberately absent: the plugin carries
      # documented pre-existing PHPCS debt (see AGENTS.md known issues).
      - name: Lint
        run: npm run lint:js

      - name: Typecheck
        run: npm run typecheck

      - name: Jest
        run: npm run test:js

      - name: Build the plugin bundle
        run: npm run build

  php:
    name: PHPUnit (wp-env)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install plugin dependencies
        run: npm ci

      - name: Install PHP dependencies
        run: composer install --no-progress

      # The PHP test path loads the subtree's PHP directly (framework
      # first, then this plugin — see tests/bootstrap.php); it does not
      # need the subtree's JS built.
      - name: Start wp-env
        run: npm run env start

      - name: PHPUnit
        run: npm run test:php

  e2e:
    name: End-to-end (Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            gutenberg/package-lock.json

      - name: Install plugin dependencies
        run: npm ci

      - name: Install and build the vendored Gutenberg subtree
        run: |
          cd gutenberg
          npm ci --ignore-scripts
          npm run build

      - name: Build the plugin bundle
        run: npm run build

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Start wp-env
        run: npm run env start

      # The @wordpress/scripts base config applies 2 retries under CI,
      # which absorbs the suite's known under-load flake (a fixture login
      # navigation can time out in a full run; every spec is green solo —
      # see AGENTS.md).
      - name: Playwright
        run: npm run test:e2e

      - name: Upload test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts
          path: artifacts/
          retention-days: 7
